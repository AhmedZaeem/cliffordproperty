enum sharedPrefrencesKeys {
  autoLogin,
  theme,
  noBoarding,
  loginFirst,
}

enum CacheKeys {
  theme,
  language,
  loggedIn,
  id,
  name,
  email,
  fcm,
  lat,
  lng,
}


enum AppPermission { user, admin }

enum FbCollection {
  users,
  categories,
  products,
  rates,
  carts,
  sliders,
  notifications,
  favorites,
}
